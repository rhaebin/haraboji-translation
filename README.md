# Haraboji Book Translation Project

Translation pipeline for grandfather's 1960s Korean book with Hanja characters.

## Setup
```bash
pip3 install -r requirements.txt
```

## Usage
1. Put photos in `photos/` folder
2. Run: `streamlit run app.py`
3. Follow the interface
